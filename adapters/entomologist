#!/bin/sh
# Contract entry point for the entomologist adapter: the program `--adapter` names.
# Resolves the binary relative to this file so the caller's cwd and PATH do not
# decide which adapter implementation reads the register.
here=$(dirname "$0")
exec "$here/../target/debug/adapter-entomologist" "$@"

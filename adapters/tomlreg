#!/bin/sh
# Contract entry point for the tomlreg adapter: the program `--adapter` names.
# Resolves the interpreter relative to this file so the caller's cwd and PATH
# do not decide which Python reads the register.
here=$(dirname "$0")
PYTHONPATH="$here/.." exec "$here/../.venv/bin/python" -m adapters.tomlreg "$@"

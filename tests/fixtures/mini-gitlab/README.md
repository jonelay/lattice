Hermetic GitLab API responses and a `glab` stub for the GitLab adapter tests.
The test fixture copies this directory into a temporary Git repository and sets
its origin to `https://gitlab.com/test-group/test-project.git`.

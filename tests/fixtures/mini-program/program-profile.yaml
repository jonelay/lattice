name: mini-program
profile_version: "1.0.0"

edge_kinds:
  traces_to:
    allowed:
      - [markdown/requirement, toml/item]

validations:
  - DANGLING_REF:
      severity: error
  - COVERAGE:
      target_kind: toml/item
      edge_kind: traces_to
      severity: hint

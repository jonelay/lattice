# Fixture register carrying a row-bearing table the adapter has no reader for.
# It must be reported: a row shape nobody reads cannot shrink the graph quietly.

[register]
id = "reg.unread"
register_version = 0
status = "draft-1"

[[item]]
id = "known"
stage = "s1"

[[note]]
id = "unread_row"
